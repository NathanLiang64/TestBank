import styled from 'styled-components';
import Layout from 'components/Layout';

const SMSOTPactivateWrapper = styled(Layout)`
  .switchContainer {
    margin-bottom: 3rem;
  }
  .infoArea {
    text-align: left;
    text-align: justify;
  }
`;

export default SMSOTPactivateWrapper;
