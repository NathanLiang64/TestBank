import styled from 'styled-components';
import { MuiPickersUtilsProvider as MaterialPickersProvider } from '@material-ui/pickers';

const FEIBPikcersProvider = styled(MaterialPickersProvider)`

`;

export default FEIBPikcersProvider;
