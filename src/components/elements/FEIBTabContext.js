import styled from 'styled-components';
import { TabContext as MaterialTabContext } from '@material-ui/lab';

/*
* ==================== FEIBTabContext 可用選項 ====================
* */
const FEIBTabContext = styled(MaterialTabContext)`

`;

export default FEIBTabContext;
